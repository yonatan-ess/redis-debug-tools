name: test-build
env:
  NAME: 'TESTingNAME'
  env_var: ${{ vars.ENV_CONTEXT_VAR }}
on:
  pull_request:
    paths:
      - '/**'
  push:
    branches:
      - 'master'
    paths:
      - '/**'
  workflow_dispatch:
  pull_request:
    types: [opened, reopened]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Get Name
        id: get-name
        working-directory: /
        run: |
          echo ""
          echo ""
          echo "Hello $NAME. ${{ vars.JOB_NAME }}"
